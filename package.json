{
  "name": "basic-nodejs",
  "version": "1.0.0",
  "description": "The event loop is a mechanism that enables JavaScript to handle asynchronous code execution in a non-blocking manner, and ensures that the execution of tasks is ordered and does not block the main thread.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "joi": "^17.9.1",
    "mongoose": "^7.0.3"
  }
}
